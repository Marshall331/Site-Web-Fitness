// Definition d'une suite de tests
describe('Prise en main Jasmine', () => {
    // Un test de la suite
    it('Mon premier test avec Jasmin test', () => {
        // Assertion (true) & matcher toBe()
        expect(true).toBe(true)
    })
    // un autre test de la même suite
    // it('2 + 2 vaut 4', () => {
    //     expect(2 + 2).not.toBe(4)
    // })
})